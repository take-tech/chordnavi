# ChordNavi（五度圏・コード進行のDAWプラグイン）

## このプロジェクトについて

製品名は **ChordNavi**（旧名 Godoken Navigator。プラグインコード `Rnze`/`Gdkn` は変えないので既存のプロジェクトはそのまま開ける）。

五度圏・スケール・ギター指板・コード進行を1画面で確認し、コードをMIDIとしてDAWへドラッグ＆ドロップできるプラグイン。
完成イメージは `reference/prototype.html`（ブラウザで動くプロトタイプ）。**UI・音楽理論ロジック・挙動はこのプロトタイプを正とする。** 迷ったらプロトタイプの挙動に合わせること。

## 技術スタック

- JUCE 8（C++17以上）、ビルドは CMake
- プラグイン形式：VST3、AU（macOSのみ）、Standalone
- UI：JUCE の `WebBrowserComponent` で HTML/JS を表示（プロトタイプを流用）
  - UIファイルはバイナリに埋め込み、`ResourceProvider` 経由で配信する（外部ファイルやネット接続に依存しない）
  - Google Fonts は使わず、フォントはOS標準のフォールバック（Hiragino Sans / Yu Gothic UI / Noto Sans JP）にする
- JS ⇔ C++ の連携は JUCE 8 のネイティブ関数（`withNativeFunction` / `window.__JUCE__.backend`）を使う

## ディレクトリ構成（目安）

```
CMakeLists.txt
reference/prototype.html      … 参照用。編集しない
Source/
  PluginProcessor.h/.cpp
  PluginEditor.h/.cpp         … WebBrowserComponent を載せる
  MidiExport.h/.cpp           … コード列 → .mid ファイル生成、外部ドラッグ
  PreviewSynth.h/.cpp         … 試聴用の簡易シンセ（三角波＋エンベロープ、ロックフリーのキュー）
  PluginState.h/.cpp          … 保存データ（UI 状態の JSON を ValueTree で包む）
  StandaloneApp.cpp           … Standalone 版のアプリ本体（macOS 標準のタイトルバー、上部メニューバーの「オプション」）
assets/icon/                  … アプリアイコンの案（SVG と 1024px PNG）
ui/
  index.html                  … プロトタイプを分割・整理したもの
  app.js / theory.js / midi.js / audio.js / staff.js / guitar.js / host.js / persist.js / style.css
```

## 機能仕様

1. **五度圏**：外周＝メジャー、内周＝マイナー。クリックしたキーが上に来るよう回転（最短方向・約750msのイージング、reduced-motion時は即時）。上部固定のオーバーレイでダイアトニックの位置と度数を表示。中央にキー名・調号。
2. **鍵盤／五線譜**：パネル内で切替。鍵盤は3オクターブ（C3〜C6）で、押すとその音を試聴（進行の試聴は止めない）。ルート／スケール音／選択中コードの構成音を色分け。「音名／度数」切替。
   - 五線譜は大譜表＋調号。通常はスケール（ト音記号、主音〜1オクターブ上）、コード選択時は MIDI と同じボイシングを和音で表示。
   - 「コード判別」オンの間は、鍵盤・指板のクリックで音を選択／解除（最大8音、紫で表示。指板は1本の弦に1音までで、同じ弦の別フレットを押すと置き換え。鍵盤で選んだ音は指板に薄い紫で表示）し、構成音からコード候補を表示（最低音をベースとして分数コード、5度省略も候補。最低音がコードの外の音なら残りの音のコードのオンコード（例：D/G）も候補）。候補はクリックで試聴、ドラッグでMIDI。
   - 五線譜の綴りは度数・音程から決める（例：F♯メジャーの7度は E♯、ダブルシャープ／フラットあり）。鍵盤・指板の表記はプロトタイプのまま。
3. **ギター指板**：レギュラーチューニング、0〜15フレット、上が1弦。表示ルールは鍵盤と同じ。押すとその音（1弦開放＝E4〜6弦開放＝E2）を試聴。
   - 「指板｜コード図｜TAB」切替。コード図は横向き（上が1弦、ローポジションはナット、それ以外は開始フレット番号を左下に、セーハは太い棒、○開放・×ミュート、ベースはオレンジ）。コード図と TAB はコードを選んでいるとその3ポジション（6弦・5弦・4弦ルートの一番良い形）、選んでいない／試聴中は進行を「ロー／ミドル／ハイ」のエリアで表示（前のコードから手の移動が少ない形）。列を押すとその形を試聴。
   - フォームは `ui/guitar.js` で探索（最低音＝ルート／分数コードのベース、3フレット以内・指4本以内（セーハは1本）、4弦以上、途中ミュート1本まで、5度は省略可、開放弦はローポジションのみ・セーハの上では不可）。
4. **スケール**：プロトタイプの14種類＋オーギュメント（対称）。加えて「コードトーン」グループ（M7・m7・7・m7♭5・dim7。五度圏で選んだキーの主音をルートに構成音だけを表示）。いずれも `SCALES`（コードトーンは `group:'chord'`）。キーのモードを切り替えたときのみ既定スケール（メジャー／ナチュラル・マイナー）に戻す。
5. **ダイアトニックコード**：7つ。「4和音／3和音」切替（既定は4和音）。切替時は選択中のコードも対応する和音に置き換える（3和音へは7thを落とす、4和音へはダイアトニックの度数に一致する場合のみ）し、その和音を試聴する。クリックで試聴＋構成音表示、ドラッグでMIDI。
6. **コード進行**：メジャー8種・マイナー5種（`PROGRESSIONS`。プロトタイプの12小節ブルース2種は削除）。進行名ボタンで1つ選び、コード名とディグリーをチップ表示。単体コード・進行全体をドラッグでMIDI。「MIDI保存」でファイル保存も可能。
   - 「その他…」ボタンから一覧で選べる追加の進行（`extra:true`、メジャー12種・マイナー5種）。その他の進行にも派生形あり（「2拍ずつ」「7th」など）。メインの進行の後ろに並べるので保存済みの番号はずれない。
   - 進行のチップを選ぶと、ヒントの位置に変更バーが出て、そのコードのルート（主音から12音。分数コードはベースとの距離を保つ）と種類を一時的に変えられる。1小節のコードは「✂ 分割」で2拍×2に、分割された小節は「結合」で1小節に戻せる（小節単位のみ）。「元に戻す」は小節ごと（試聴・ドラッグ・保存に反映、ファイル名に `_custom`、チップに紫の印）。進行・派生を切り替えると元に戻る。保存しない。
   - 各進行に厳選した派生形（`v`）があり、進行の下の「派生」ボタンで切替。分数コード（例：G/B）や、1小節に2コード（例：Vm7・I7）を含む。チップの幅は拍数に比例（1小節＝4マス）。
7. **表記**：フラット系キー（五度圏の位置7〜11）は♭表記。コード構成音はコードのルート基準で綴る（例：C7の7度はB♭）。キーの外から借りたコード（♭II・♭VI・♭VII・♯IV など）のルートは度数から綴る（例：C キーの ♭VII は B♭、♭II7 は D♭7）。E♯・B♯・C♭・F♭ やダブル♯／♭になる場合はキーの表記に戻す（`chordRootName`）。

8. **スケールに沿う**：コード進行欄のボタン。オンのとき、メジャー・ナチュラル／ハーモニック／メロディック・マイナー・ドリアン・フリジアン・リディアン・ミクソリディアン・ロクリアンでは、進行とダイアトニックコードを「同じ度数・同じ構成（3度・5度・7度…）のまま選んだスケールの音」で作り直す（`conformBars` / `diatonicOf`）。元のスケール（メジャーの進行はメジャー、マイナーの進行はナチュラル・マイナー）の外の音を含むコード（セカンダリードミナント・借用・裏コード）は元のまま。それ以外のスケールでは無効。作り直したコードは度数番号（deg）を持ち、ロクリアンの ♭V などを正しく表記する。
9. **MIDI キーボード入力**：DAW のトラック／Standalone の MIDI 入力（自動で開く）を試聴シンセで鳴らす（音色メニューと同じ、ベロシティ・サステインペダル対応）。押している音を 30Hz で UI に送り、鍵盤・指板・五線譜を青く光らせ、見出しにコード名を出す。表示範囲外の音はオクターブを折り返す。

## UI レイアウト

- 基準サイズ 1280×780 の固定ステージを、ウィンドウに合わせて縦横比を保ったまま拡縮する。**スクロールは絶対に出さない。**
- プラグインウィンドウはリサイズ可能にし、縦横比 1280:780 を固定、最小 960×585。
- 配色・フォントサイズ・配置はプロトタイプに合わせる。
- テーマは「ライト／ダーク／自動（OS の外観）」。Standalone はメニューの「オプション → テーマ」（C++ → JS の `setTheme` イベント、今のテーマは `reportTheme` で C++ へ）、DAW 上はタイトル横の小さなボタン（押すと順に切り替え）。色は `style.css` の CSS 変数にまとめ、`[data-theme=dark]` で上書きする。SVG の色も `el()` に `var(--…)` を渡して style で指定する（切り替えで描き直し不要）。ルート・スケール音・コード構成音・MIDI 入力などの意味を持つ色はテーマで変えない。
- グリッドの列は `minmax(0,1fr)` にする（`1fr` だと中身の文字幅で列が広がり、画面の右がはみ出す）。長い進行のディグリーは進行名の下の行に出す（その間ヒントは隠す）。

## MIDI 仕様

- テンポは DAW 上では既定で DAW に同期（「DAW同期」ボタンで解除可、Standalone では非表示）。同期中はテンポ欄に DAW の値を表示し、試聴・ドラッグ・保存に使う（小数可、20〜300）。
- SMF フォーマット0、分解能480、1コード＝1小節（4/4。1小節に複数コードの進行は小節内で等分、例：Vm7・I7 は各2拍。単体コードは常に1小節）、テンポはUIの入力値（40〜240、既定120）。
- ボイシング：ベース＝ルート（分数コードは指定のベース音）を C2〜B2、上声＝ルートをC3〜B3に置いてコードトーンを積む。ベロシティ90。
- ファイル名：**日本語を使わない**。進行は `キー_進行の英字名[_派生の英字名][_half]`（例 `C_RoyalRoad.mid`、`Am_Andalusian_BassLine.mid`、`C_Canon_half.mid`）、単体 `FM7.mid`（♯→#、♭→b に置換）。英字名は theory.js の `file` / `VARIANT_FILE`。

## MIDI ドラッグ＆ドロップ（最重要・最大の技術リスク）

ブラウザ版の `DownloadURL` はプラグイン内の WebView では使えないため、以下の方式にする。

1. JS 側でチップ上の mousedown ＋ 数px の移動を検知したら、コード列をネイティブ関数へ渡す
2. C++ 側で一時フォルダに .mid を書き出す
3. `DragAndDropContainer::performExternalDragDropOfFiles` でOSのファイルドラッグを開始する

WebView上のマウス操作から外部ドラッグを開始できるかはOS・DAWで挙動が異なる可能性がある。**最初のマイルストーンで必ず検証**し、うまくいかない場合は、ドラッグ用のつまみだけをネイティブJUCEコンポーネントとしてWebViewの上に重ねる方式に切り替えること。切り替える前に相談すること。

## 試聴

- プラグインでは WebAudio ではなく、C++ 側の簡易シンセで `processBlock` から鳴らす。
- 音色は「シンプル（三角波＋エンベロープ、プロトタイプと同じ）／ピアノ（倍音加算）／エレピ（FM）／ギター（Karplus-Strong、低音から順にストローク）／オルガン（ドローバー風、減衰なし）／パッド（デチューンしたノコギリ波＋ローパス、減衰なし）」から選ぶ。試聴のみに影響し、MIDI 出力は変わらない。
- 新しく試聴を始めると、鳴っている音・予約中の音は止める（10ms フェード）。
- 進行の試聴は UI のテンポで MIDI と同じ長さ（1小節＝4拍、小節内に複数コードなら等分）。単体コードは 1.1 秒。
- 「リズム½」で進行の各コードの長さを半分にする（試聴・ドラッグ・保存。ファイル名に `_half`）。
- 進行の試聴は「ループ」で繰り返せる。再生中は「試聴」ボタンが「停止」になる。音色の既定はオルガン。
- 進行の試聴中は、鳴っているコードのチップを着色し、鍵盤・指板・五線譜にそのコードを表示する（終了後は元の表示に戻る）。キー・進行・派生を切り替えたら音も止める。
- Standalone でも同じく鳴ること。

## 状態の保存

- UI の状態を JSON にして C++ に預け（`saveState`）、`getStateInformation` で保存する。`setStateInformation` で復元したら、開いている UI に `stateRestored` で反映する。
- 保存するもの：キー・モード・スケール・音名／度数・鍵盤／五線譜・4和音／3和音・選択中の進行と派生（メジャー／マイナー別）・テンポ（手入力値）・DAW同期・音色・ループ・リズム½。
- 保存しないもの：選択中のコード、コード判別の選択音、再生状態。
- 読み込んだ値は1つずつ検証し、不正な値は既定値のままにする。

## マイルストーン（この順で進め、各段階で動作確認してから次へ）

1. JUCE の CMake プロジェクト作成、空の WebView を表示する Standalone がビルド・起動できる
2. **MIDI ドラッグの検証**：固定のCメジャーコード1つを WebView のボタンから DAW／デスクトップへドラッグできる
3. プロトタイプの UI を `ui/` に移植し、埋め込みリソースとして表示（外部通信なし）
4. JS ⇔ C++ 連携：MIDI生成とドラッグ、MIDI保存（ネイティブのファイル保存ダイアログ）
5. C++ 側の試聴シンセ
6. 状態の保存（キー・モード・スケール・表示・選択中の進行・テンポを `getStateInformation` で保存／復元）
7. VST3／AU のビルドと、DAW上での動作確認

## ビルド・確認

```
# 構成（手元の JUCE を使う場合は -DFETCHCONTENT_SOURCE_DIR_JUCE=<JUCE のパス>）
cmake -S . -B build-release -DCMAKE_BUILD_TYPE=Release
cmake --build build-release --target ChordNavi_VST3 ChordNavi_AU ChordNavi_Standalone -j8

# 単体テスト
cmake --build build --target MidiExportTests && ./build/MidiExportTests_artefacts/Debug/MidiExportTests

# インストール（ユーザーのプラグインフォルダ）と AU の検証
cp -R "build-release/ChordNavi_artefacts/Release/VST3/ChordNavi.vst3" ~/Library/Audio/Plug-Ins/VST3/
cp -R "build-release/ChordNavi_artefacts/Release/AU/ChordNavi.component" ~/Library/Audio/Plug-Ins/Components/
auval -v aumu Gdkn Rnze
```

- 配布用 `.pkg`：`./scripts/package_macos.sh`（`build-release/packages/ChordNavi-<ver>-macOS.pkg`。VST3・AU・Standalone を入れる。バージョンは CMake の `project(... VERSION)`、または環境変数 `CHORDNAVI_VERSION`）。署名・公証は環境変数（`MACOS_APP_SIGN_IDENTITY` など）があるときだけ。
- ビルドは Intel／Apple Silicon のユニバーサル（`CMAKE_OSX_ARCHITECTURES`、macOS 11 以降）。
- タグ `v*` を push すると `.github/workflows/release.yml` が `.pkg`（macOS）と `Setup.exe`（Windows）をビルドして GitHub Release に添付する。手動実行（workflow_dispatch）では成果物だけ作る。
- **Windows**：GitHub Actions（windows-2022）でビルド。WebView2 SDK を NuGet で取得し `-DJUCE_WEBVIEW2_PACKAGE_LOCATION` で渡す。WebView2 は静的リンク（`JUCE_USE_WIN_WEBVIEW2_WITH_STATIC_LINKING`）、エディタは WebView2 バックエンド＋データフォルダを AppData に。MSVC は `/utf-8`。インストーラーは Inno Setup（`installer/windows/ChordNavi.iss`、VST3 と Standalone、WebView2 ランタイムが無ければ案内）。Standalone の「オプション」はウィンドウ内のメニューバー。

- 音源（aumu）なので MIDI 入力を受ける設定にしている（`NEEDS_MIDI_INPUT TRUE`。auval の MIDI テストに必要）。受けた MIDI は今は使わない。
- UI をブラウザで確認するときは `ui/` を HTTP で配信して開く（JUCE ブリッジが無いときは WebAudio・DAW同期なしで動く）。

## ルール

- 音楽理論のデータ（スケール・コード・進行）は `theory.js` に集約し、UIコードと混ぜない。
- MIDI生成ロジックには単体テストを書く（ヘッダ、デルタタイム、ノートオン／オフの対応）。試聴シンセにもテストあり（`tests/`、ターゲット `MidiExportTests`）。
- 大きな設計変更や方式の切り替えは、実装前に提案して確認を取る。
- コミットはマイルストーン単位を目安に、日本語のメッセージで。
